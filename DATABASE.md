# Database

Superseded. The schema documentation now lives at
[`docs/architecture/database-schema.md`](docs/architecture/database-schema.md), which follows the
field naming in `prisma/schema.prisma`.

This file previously held a from-scratch field list written before that schema existed. It used
different table and column names, so it has been replaced rather than kept alongside — two
schema documents drift, and a drifted one is worse than none.
